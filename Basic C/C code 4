#include <stdio.h>
int  main() {
    float length,breadth,area;
    printf("Enter a length: ");
    scanf("%f", &length);
    printf("Enter a breadth: ");
    scanf("%f", &breadth);
    area = length * breadth;
    printf("Area of the rectangle: %.2f\n", area);
    return 0;
}